import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.my.nandakho',
  appName: 'nandakho.my.id',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
